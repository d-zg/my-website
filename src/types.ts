export interface CardHolderProps {
    title: String;
    description: String,
    link: String,
}

export interface BlogPost {
    _id: String,
    tagline: String,
    headline: String,
    image: String,
    fulltext: String,
}
